{assign var="page_title" value="this is the part where we do the invitation code dance"}
{include file="inc_head.txt"}

<div id="notphotos">

{if $step=='request'}

<form method="POST" action="{$cfg.abs_root_url}invite/request/">
	{$crumb_key|crumb_input}
	<input type="hidden" name="request" value="1" />
	<input type="text" name="email" size="40" /> email<br />
	<input type="submit" value="I WOULD LIKE VERY MUCH TO PLAY" />
</form>

{elseif $step=='requested'}

{if $error|@count}
<p class="error">Well this is embarrasing. There was a problem registering your request for an invite. We'll look in to it. Sorry...</p>
{else}
<p>Okay! Thanks for your request. We'll send you an email when the next round of invites go out.</p>
{/if}

{elseif $step=='submit'}

{if $error|@count}
<p class="error">Hrm. There seems to be a problem with that invite code. Here's what the robots told me: {$error.details|escape}</p>
{/if}

{else}

<form method="POST" action="{$cfg.abs_root_url}invite/">
	{$crumb_key|crumb_input}
	<input type="hidden" name="submit" value="1" />
	<input type="text" name="email" size="40" /> email<br />
	<input type="text" name="code" size="40" /> code<br />
	<input type="submit" value="LET ME IN!" />
</form>

	<p>Don't have an invite code? <a href="{$cfg.abs_root_url}invite/request/">You can request one over here.</a></p>
{/if}

</div>

{include file="inc_head.txt"}
